<main class="mx-auto">
    <div>
        {{# hotProducts}}
            <h2 class="flex text-4xl mb-4 mt-10 uppercase font-bold w-fit "><img src="/api/public/svg/flame.svg" class="size-12 [filter:invert(44%)_sepia(96%)_saturate(3938%)_hue-rotate(360deg)_brightness(102%)_contrast(104%)]" />Les produits tendances !</h2>
            {{> card}}
        {{/ hotProducts}}
    </div>
    <h2 class="flex text-4xl mb-4 mt-10 uppercase font-bold w-fit ">Tous nos produits</h2>
    <div class="space-x-5 my-4 grid grid-cols-6 gap-4 mx-auto">
        {{# products}}
            {{> card}}
        {{/ products}}
    </div>
</main>